// RibeyeSpecial.cpp : This file contains the 'main' function. Program execution begins and ends there.
//
#include <Windows.h>

// Data that will be replaced by 
const char rawData[5000000] = { 'C', 'O', 'C', 'O', 'N', 'U', 'T' };

struct Coconut
{
	DWORD AES_Key = 0;
	BOOLEAN prime = FALSE;
	BOOLEAN mouse = FALSE;
	BOOLEAN sleeptimer = FALSE;
};

int main()
{


}